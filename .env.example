# API Configuration
# For local development (backend must be running on port 3001)
VITE_API_URL=http://localhost:3001

# Note: When deployed to Vercel, the API URL is automatically configured
# via vercel.json to use relative paths (/api)
